<!doctype html>
<html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ $title ?? 'Impulso' }} | Impulso</title><link rel="icon" type="image/svg+xml" href="{{ asset('favicon.svg') }}">@vite(['resources/css/app.css', 'resources/js/app.js'])</head>
<body><nav class="nav"><a class="brand" href="{{ route('home') }}"><span class="brand-mark">+</span> impulso</a><div class="nav-links"><a href="{{ route('discover') }}">Explorar</a>@auth <a href="{{ route('inbox.index') }}">Bandeja</a>@php($hasBusinessAccess = auth()->user()->ownedBusinesses()->exists() || auth()->user()->businesses()->exists()) @if($hasBusinessAccess)<a href="{{ route('business.panels') }}">Mis emprendimientos</a>@else<a href="{{ route('business.create') }}">Crear emprendimiento</a>@endif<form method="POST" action="{{ route('logout') }}"><button class="link-button">Salir</button>@csrf</form>@else<a href="{{ route('login') }}">Ingresar</a><a class="button button-small" href="{{ route('register') }}">Crear cuenta</a>@endauth</div></nav><main>@if(session('success'))<div class="notice success">{{ session('success') }}</div>@endif @yield('content')</main><footer><span class="brand"><span class="brand-mark">+</span> impulso</span><span>Herramientas simples para ideas que crecen.</span></footer></body></html>