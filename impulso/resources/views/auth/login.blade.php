@extends('layouts.app')
@section('content')<section class="auth-page"><div class="auth-panel"><p class="eyebrow">Qué bueno verte</p><h1>Ingresa a tu espacio.</h1><p class="muted">Continúa organizando tu emprendimiento.</p><form method="POST" action="{{ route('login.store') }}" class="form">@csrf<label>Correo electrónico<input type="email" name="email" value="{{ old('email') }}" required autofocus></label>@error('email')<small class="error">{{ $message }}</small>@enderror<label>Contraseña<input type="password" name="password" required></label><label class="check"><input type="checkbox" name="remember"> Recordarme</label><button class="button full">Ingresar <span>→</span></button></form><p class="form-foot">¿Todavía no tienes cuenta? <a href="{{ route('register') }}">Crea una gratis</a></p></div><div class="auth-note"><span class="quote">“</span><p>Tu negocio no tiene que sentirse complicado. Empieza por un paso.</p><small>— La comunidad Impulso</small></div></section>@endsection