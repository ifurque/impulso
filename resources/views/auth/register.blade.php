@extends('layouts.app')
@section('content')<section class="auth-page"><div class="auth-panel"><p class="eyebrow">Tu próximo paso</p><h1>Crea tu cuenta.</h1><p class="muted">Explora la comunidad o comienza tu propio emprendimiento.</p><form method="POST" action="{{ route('register.store') }}" class="form">@csrf<label>Nombre completo<input type="text" name="name" value="{{ old('name') }}" required autofocus></label>@error('name')<small class="error">{{ $message }}</small>@enderror<label>Correo electrónico<input type="email" name="email" value="{{ old('email') }}" required></label>@error('email')<small class="error">{{ $message }}</small>@enderror<label>Contraseña<input type="password" name="password" required></label><label>Repite tu contraseña<input type="password" name="password_confirmation" required></label>@error('password')<small class="error">{{ $message }}</small>@enderror<button class="button full">Crear mi cuenta <span>→</span></button></form><p class="form-foot">¿Ya tienes cuenta? <a href="{{ route('login') }}">Ingresar</a></p></div><div class="auth-note warm"><span class="quote">✦</span><p>Una comunidad de personas que hacen que las cosas pasen.</p><small>Encuentra, conecta, crece.</small></div></section>@endsection